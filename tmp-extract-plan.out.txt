===== user line 3583 =====

--- One-Time Signup Fee @5405 ---
 ACCOUNT SETUP

Fields should include the current required company/contact fields plus:

- Company / Organization
- Card / Person Creation Limit
- Manage Access
- Agreed Monthly Subscription Amount
- One-Time Signup Fee
- Stripe billing/payment-link area

Corporate enters:

CORPORATE BACK OFFICE

==================================================
7. GLOBAL PACKAGE MANAGEMENT
==================================================

The EXISTING Packages Management area remains the global source of truth.

Current packages include:

Free
Professional
Professional Concierge
Corporate

Current Package Manager already contains things such as:

- Name
- Slug
- Stripe Price ID
- Monthly Price
- Features
- Status
- Order

and current package features such as:

max_social_links
max_extra_fields
allow_video_upload
allow_music_upload
allow_2d_explainer
max_file_size_mb
allow_yt_bg_music_upload
allow_bg_music_upload
allow_intro_video_upload
allow_background_video_upload

READ the actual current schema/UI.

Do NOT create a second Package Manager.

==================================================
8. GLOBAL PACKAGE FEATURES
==================================================

Package Management determines GLOBAL DEFAULT access.

Example:

FREE:
limited features

PROFESSIONAL:
more access

PROFESSIONAL CONCIERGE:
higher Single-owner access

CORPORATE:
global Corporate defaults

Package feature types currently include concepts such as:

BOOLEAN
true / false

NUMERIC
5 / 10 / 30

UNLIMITED
no limit

Preserve/reuse the current flexible package-feature model.

Do NOT represent Unlimited using fake values such as 999999.

==================================================
9. CURRENT PACKAGE MUST CONTROL ACCESS
==================================================

This is critical.

Do NOT permane

--- One-Time Signup Fee @18760 ---
=====================================

When Corporate is selected show:

CORPORATE ACCOUNT SETUP

Company
[________]

Card / Person Limit
[25]

Manage Access
[Configure]

Monthly Subscription
[$200]

One-Time Signup Fee
[$10]

Initial Amount
[$210 automatically calculated]

Future Billing
$200/month

[Create Corporate Account / Generate Payment Link]

Use the existing screen instead of building a separate Corporate creation system.

==================================================
32. CORPORATE STRIPE BILLING
==================================================

The backend should generate the Stripe billing/payment link.

Frontend does NOT create Stripe Products/Prices directly.

Backend uses the Admin-approved Corporate billing terms.

Desired billing:

Initial:
one-time setup/signup fee
+
recurring monthly subscription

Future:
recurring monthly subscription only

Inspect the CURRENT Stripe architecture before implementing.

Reuse:

Stripe customer
Stripe checkout
Stripe payment links
Stripe prices
Stripe subscriptions
Stripe webhooks

where appropriate.

==================================================
33. CORPORATE CUSTOM STRIPE PAYMENT LINK
==================================================

After Admin enters negotiated terms:

[Generate Stripe Payment Link]

Backend creates a customer/agreement-specific Stripe checkout/payment link.

Store/link actual internal metadata such as:

corporateAccountId
packageId
billingAgreementId

Use current naming.

Do NOT trust IDs or prices sent from a customer's browser.

==================================================
34. STRIPE WEBHOOK IS PAYMENT TRUTH
==================================================

Do not mark payment/subscription ACTIVE because the browser reaches a success URL.

Stripe webhook confirmation is the 

--- GLOBAL PAC @5557 ---
e Access
- Agreed Monthly Subscription Amount
- One-Time Signup Fee
- Stripe billing/payment-link area

Corporate enters:

CORPORATE BACK OFFICE

==================================================
7. GLOBAL PACKAGE MANAGEMENT
==================================================

The EXISTING Packages Management area remains the global source of truth.

Current packages include:

Free
Professional
Professional Concierge
Corporate

Current Package Manager already contains things such as:

- Name
- Slug
- Stripe Price ID
- Monthly Price
- Features
- Status
- Order

and current package features such as:

max_social_links
max_extra_fields
allow_video_upload
allow_music_upload
allow_2d_explainer
max_file_size_mb
allow_yt_bg_music_upload
allow_bg_music_upload
allow_intro_video_upload
allow_background_video_upload

READ the actual current schema/UI.

Do NOT create a second Package Manager.

==================================================
8. GLOBAL PACKAGE FEATURES
==================================================

Package Management determines GLOBAL DEFAULT access.

Example:

FREE:
limited features

PROFESSIONAL:
more access

PROFESSIONAL CONCIERGE:
higher Single-owner access

CORPORATE:
global Corporate defaults

Package feature types currently include concepts such as:

BOOLEAN
true / false

NUMERIC
5 / 10 / 30

UNLIMITED
no limit

Preserve/reuse the current flexible package-feature model.

Do NOT represent Unlimited using fake values such as 999999.

==================================================
9. CURRENT PACKAGE MUST CONTROL ACCESS
==================================================

This is critical.

Do NOT permanently copy package permissions into the User when the account is created.

At authentication and protected-action time, the backend must determine the us

--- GLOBAL PAC @6305 ---
music_upload
allow_intro_video_upload
allow_background_video_upload

READ the actual current schema/UI.

Do NOT create a second Package Manager.

==================================================
8. GLOBAL PACKAGE FEATURES
==================================================

Package Management determines GLOBAL DEFAULT access.

Example:

FREE:
limited features

PROFESSIONAL:
more access

PROFESSIONAL CONCIERGE:
higher Single-owner access

CORPORATE:
global Corporate defaults

Package feature types currently include concepts such as:

BOOLEAN
true / false

NUMERIC
5 / 10 / 30

UNLIMITED
no limit

Preserve/reuse the current flexible package-feature model.

Do NOT represent Unlimited using fake values such as 999999.

==================================================
9. CURRENT PACKAGE MUST CONTROL ACCESS
==================================================

This is critical.

Do NOT permanently copy package permissions into the User when the account is created.

At authentication and protected-action time, the backend must determine the user's
CURRENT active/assigned package.

Conceptually:

User
↓
Current Active Subscription / Package Assignment
↓
Package
↓
Package Features
↓
Effective Entitlements
↓
Access

If Admin upgrades:

Professional
→ Professional Concierge

the user should receive Concierge access without manually changing every feature.

If package settings change globally:

users on that package should receive the new effective access.

==================================================
10. CENTRAL ENTITLEMENT SERVICE
==================================================

Create/reuse ONE backend service that is the authority for access.

Conceptually:

getEffectiveEntitlements(userId, profileId?)

SINGLE:

User
→ Current Package
→ Global Package Features

CORPORA

--- $10 @17645 ---
g.

Use the existing Stripe structure.

There is also a globally configured one-time signup/setup fee for applicable paid Single plans.

Current business requirement discussed:

one-time signup fee = $10

Do NOT scatter literal $10 values throughout code.

Store/reuse centralized configuration.

Example:

Professional monthly = package price

FIRST PAYMENT:
one-time signup fee
+
first monthly subscription

FUTURE:
monthly subscription only

Professional Concierge behaves similarly.

Free should not accidentally create paid recurring billing.

==================================================
30. CORPORATE BILLING IS NEGOTIATED
==================================================

Corporate is:

CONTACT FOR SALES

Corporate does not need a fixed public retail price.

Admin negotiates:

- monthly subscription amount
- one-time signup fee
- card/person limit
- feature overrides

Example:

Corporate:
Monthly subscription = $200
Signup Fee = $10
Card Limit = 25

FIRST PAYMENT:
$210

FUTURE MONTHS:
$200/month

==================================================
31. CORPORATE ADMIN CREATION UI
==================================================

When Corporate is selected show:

CORPORATE ACCOUNT SETUP

Company
[________]

Card / Person Limit
[25]

Manage Access
[Configure]

Monthly Subscription
[$200]

One-Time Signup Fee
[$10]

Initial Amount
[$210 automatically calculated]

Future Billing
$200/month

[Create Corporate Account / Generate Payment Link]

Use the existing screen instead of building a separate Corporate creation system.

==================================================
32. CORPORATE STRIPE BILLING
==================================================

The backend should generate the Stripe billing/payment link.

Frontend does NOT create Stripe Products/Prices direct

--- $10 @17673 ---
structure.

There is also a globally configured one-time signup/setup fee for applicable paid Single plans.

Current business requirement discussed:

one-time signup fee = $10

Do NOT scatter literal $10 values throughout code.

Store/reuse centralized configuration.

Example:

Professional monthly = package price

FIRST PAYMENT:
one-time signup fee
+
first monthly subscription

FUTURE:
monthly subscription only

Professional Concierge behaves similarly.

Free should not accidentally create paid recurring billing.

==================================================
30. CORPORATE BILLING IS NEGOTIATED
==================================================

Corporate is:

CONTACT FOR SALES

Corporate does not need a fixed public retail price.

Admin negotiates:

- monthly subscription amount
- one-time signup fee
- card/person limit
- feature overrides

Example:

Corporate:
Monthly subscription = $200
Signup Fee = $10
Card Limit = 25

FIRST PAYMENT:
$210

FUTURE MONTHS:
$200/month

==================================================
31. CORPORATE ADMIN CREATION UI
==================================================

When Corporate is selected show:

CORPORATE ACCOUNT SETUP

Company
[________]

Card / Person Limit
[25]

Manage Access
[Configure]

Monthly Subscription
[$200]

One-Time Signup Fee
[$10]

Initial Amount
[$210 automatically calculated]

Future Billing
$200/month

[Create Corporate Account / Generate Payment Link]

Use the existing screen instead of building a separate Corporate creation system.

==================================================
32. CORPORATE STRIPE BILLING
==================================================

The backend should generate the Stripe billing/payment link.

Frontend does NOT create Stripe Products/Prices directly.

Backend uses the Admin-

--- $10 @18395 ---
xed public retail price.

Admin negotiates:

- monthly subscription amount
- one-time signup fee
- card/person limit
- feature overrides

Example:

Corporate:
Monthly subscription = $200
Signup Fee = $10
Card Limit = 25

FIRST PAYMENT:
$210

FUTURE MONTHS:
$200/month

==================================================
31. CORPORATE ADMIN CREATION UI
==================================================

When Corporate is selected show:

CORPORATE ACCOUNT SETUP

Company
[________]

Card / Person Limit
[25]

Manage Access
[Configure]

Monthly Subscription
[$200]

One-Time Signup Fee
[$10]

Initial Amount
[$210 automatically calculated]

Future Billing
$200/month

[Create Corporate Account / Generate Payment Link]

Use the existing screen instead of building a separate Corporate creation system.

==================================================
32. CORPORATE STRIPE BILLING
==================================================

The backend should generate the Stripe billing/payment link.

Frontend does NOT create Stripe Products/Prices directly.

Backend uses the Admin-approved Corporate billing terms.

Desired billing:

Initial:
one-time setup/signup fee
+
recurring monthly subscription

Future:
recurring monthly subscription only

Inspect the CURRENT Stripe architecture before implementing.

Reuse:

Stripe customer
Stripe checkout
Stripe payment links
Stripe prices
Stripe subscriptions
Stripe webhooks

where appropriate.

==================================================
33. CORPORATE CUSTOM STRIPE PAYMENT LINK
==================================================

After Admin enters negotiated terms:

[Generate Stripe Payment Link]

Backend creates a customer/agreement-specific Stripe checkout/payment link.

Store/link actual internal metadata such as:

corporateAccountId
pack

--- signup fee @17632 ---
 IDs / pricing.

Use the existing Stripe structure.

There is also a globally configured one-time signup/setup fee for applicable paid Single plans.

Current business requirement discussed:

one-time signup fee = $10

Do NOT scatter literal $10 values throughout code.

Store/reuse centralized configuration.

Example:

Professional monthly = package price

FIRST PAYMENT:
one-time signup fee
+
first monthly subscription

FUTURE:
monthly subscription only

Professional Concierge behaves similarly.

Free should not accidentally create paid recurring billing.

==================================================
30. CORPORATE BILLING IS NEGOTIATED
==================================================

Corporate is:

CONTACT FOR SALES

Corporate does not need a fixed public retail price.

Admin negotiates:

- monthly subscription amount
- one-time signup fee
- card/person limit
- feature overrides

Example:

Corporate:
Monthly subscription = $200
Signup Fee = $10
Card Limit = 25

FIRST PAYMENT:
$210

FUTURE MONTHS:
$200/month

==================================================
31. CORPORATE ADMIN CREATION UI
==================================================

When Corporate is selected show:

CORPORATE ACCOUNT SETUP

Company
[________]

Card / Person Limit
[25]

Manage Access
[Configure]

Monthly Subscription
[$200]

One-Time Signup Fee
[$10]

Initial Amount
[$210 automatically calculated]

Future Billing
$200/month

[Create Corporate Account / Generate Payment Link]

Use the existing screen instead of building a separate Corporate creation system.

==================================================
32. CORPORATE STRIPE BILLING
==================================================

The backend should generate the Stripe billing/payment link.

Frontend does NOT create Stripe Products/

--- signup fee @17814 ---
cussed:

one-time signup fee = $10

Do NOT scatter literal $10 values throughout code.

Store/reuse centralized configuration.

Example:

Professional monthly = package price

FIRST PAYMENT:
one-time signup fee
+
first monthly subscription

FUTURE:
monthly subscription only

Professional Concierge behaves similarly.

Free should not accidentally create paid recurring billing.

==================================================
30. CORPORATE BILLING IS NEGOTIATED
==================================================

Corporate is:

CONTACT FOR SALES

Corporate does not need a fixed public retail price.

Admin negotiates:

- monthly subscription amount
- one-time signup fee
- card/person limit
- feature overrides

Example:

Corporate:
Monthly subscription = $200
Signup Fee = $10
Card Limit = 25

FIRST PAYMENT:
$210

FUTURE MONTHS:
$200/month

==================================================
31. CORPORATE ADMIN CREATION UI
==================================================

When Corporate is selected show:

CORPORATE ACCOUNT SETUP

Company
[________]

Card / Person Limit
[25]

Manage Access
[Configure]

Monthly Subscription
[$200]

One-Time Signup Fee
[$10]

Initial Amount
[$210 automatically calculated]

Future Billing
$200/month

[Create Corporate Account / Generate Payment Link]

Use the existing screen instead of building a separate Corporate creation system.

==================================================
32. CORPORATE STRIPE BILLING
==================================================

The backend should generate the Stripe billing/payment link.

Frontend does NOT create Stripe Products/Prices directly.

Backend uses the Admin-approved Corporate billing terms.

Desired billing:

Initial:
one-time setup/signup fee
+
recurring monthly subscription

Future:
recurring m

--- signup fee @18281 ---
==================================================

Corporate is:

CONTACT FOR SALES

Corporate does not need a fixed public retail price.

Admin negotiates:

- monthly subscription amount
- one-time signup fee
- card/person limit
- feature overrides

Example:

Corporate:
Monthly subscription = $200
Signup Fee = $10
Card Limit = 25

FIRST PAYMENT:
$210

FUTURE MONTHS:
$200/month

==================================================
31. CORPORATE ADMIN CREATION UI
==================================================

When Corporate is selected show:

CORPORATE ACCOUNT SETUP

Company
[________]

Card / Person Limit
[25]

Manage Access
[Configure]

Monthly Subscription
[$200]

One-Time Signup Fee
[$10]

Initial Amount
[$210 automatically calculated]

Future Billing
$200/month

[Create Corporate Account / Generate Payment Link]

Use the existing screen instead of building a separate Corporate creation system.

==================================================
32. CORPORATE STRIPE BILLING
==================================================

The backend should generate the Stripe billing/payment link.

Frontend does NOT create Stripe Products/Prices directly.

Backend uses the Admin-approved Corporate billing terms.

Desired billing:

Initial:
one-time setup/signup fee
+
recurring monthly subscription

Future:
recurring monthly subscription only

Inspect the CURRENT Stripe architecture before implementing.

Reuse:

Stripe customer
Stripe checkout
Stripe payment links
Stripe prices
Stripe subscriptions
Stripe webhooks

where appropriate.

==================================================
33. CORPORATE CUSTOM STRIPE PAYMENT LINK
==================================================

After Admin enters negotiated terms:

[Generate Stripe Payment Link]

Backend creates a customer/agree

--- Manage Access @5352 ---
he Admin Create User form expands to show:

CORPORATE ACCOUNT SETUP

Fields should include the current required company/contact fields plus:

- Company / Organization
- Card / Person Creation Limit
- Manage Access
- Agreed Monthly Subscription Amount
- One-Time Signup Fee
- Stripe billing/payment-link area

Corporate enters:

CORPORATE BACK OFFICE

==================================================
7. GLOBAL PACKAGE MANAGEMENT
==================================================

The EXISTING Packages Management area remains the global source of truth.

Current packages include:

Free
Professional
Professional Concierge
Corporate

Current Package Manager already contains things such as:

- Name
- Slug
- Stripe Price ID
- Monthly Price
- Features
- Status
- Order

and current package features such as:

max_social_links
max_extra_fields
allow_video_upload
allow_music_upload
allow_2d_explainer
max_file_size_mb
allow_yt_bg_music_upload
allow_bg_music_upload
allow_intro_video_upload
allow_background_video_upload

READ the actual current schema/UI.

Do NOT create a second Package Manager.

==================================================
8. GLOBAL PACKAGE FEATURES
==================================================

Package Management determines GLOBAL DEFAULT access.

Example:

FREE:
limited features

PROFESSIONAL:
more access

PROFESSIONAL CONCIERGE:
higher Single-owner access

CORPORATE:
global Corporate defaults

Package feature types currently include concepts such as:

BOOLEAN
true / false

NUMERIC
5 / 10 / 30

UNLIMITED
no limit

Preserve/reuse the current flexible package-feature model.

Do NOT represent Unlimited using fake values such as 999999.

==================================================
9. CURRENT PACKAGE MUST CONTROL ACCESS
================================

--- Manage Access @18704 ---
==========
31. CORPORATE ADMIN CREATION UI
==================================================

When Corporate is selected show:

CORPORATE ACCOUNT SETUP

Company
[________]

Card / Person Limit
[25]

Manage Access
[Configure]

Monthly Subscription
[$200]

One-Time Signup Fee
[$10]

Initial Amount
[$210 automatically calculated]

Future Billing
$200/month

[Create Corporate Account / Generate Payment Link]

Use the existing screen instead of building a separate Corporate creation system.

==================================================
32. CORPORATE STRIPE BILLING
==================================================

The backend should generate the Stripe billing/payment link.

Frontend does NOT create Stripe Products/Prices directly.

Backend uses the Admin-approved Corporate billing terms.

Desired billing:

Initial:
one-time setup/signup fee
+
recurring monthly subscription

Future:
recurring monthly subscription only

Inspect the CURRENT Stripe architecture before implementing.

Reuse:

Stripe customer
Stripe checkout
Stripe payment links
Stripe prices
Stripe subscriptions
Stripe webhooks

where appropriate.

==================================================
33. CORPORATE CUSTOM STRIPE PAYMENT LINK
==================================================

After Admin enters negotiated terms:

[Generate Stripe Payment Link]

Backend creates a customer/agreement-specific Stripe checkout/payment link.

Store/link actual internal metadata such as:

corporateAccountId
packageId
billingAgreementId

Use current naming.

Do NOT trust IDs or prices sent from a customer's browser.

==================================================
34. STRIPE WEBHOOK IS PAYMENT TRUTH
==================================================

Do not mark payment/subscription ACTIVE because the browser rea
===== user line 3584 =====

--- One-Time Signup Fee @5405 ---
 ACCOUNT SETUP

Fields should include the current required company/contact fields plus:

- Company / Organization
- Card / Person Creation Limit
- Manage Access
- Agreed Monthly Subscription Amount
- One-Time Signup Fee
- Stripe billing/payment-link area

Corporate enters:

CORPORATE BACK OFFICE

==================================================
7. GLOBAL PACKAGE MANAGEMENT
==================================================

The EXISTING Packages Management area remains the global source of truth.

Current packages include:

Free
Professional
Professional Concierge
Corporate

Current Package Manager already contains things such as:

- Name
- Slug
- Stripe Price ID
- Monthly Price
- Features
- Status
- Order

and current package features such as:

max_social_links
max_extra_fields
allow_video_upload
allow_music_upload
allow_2d_explainer
max_file_size_mb
allow_yt_bg_music_upload
allow_bg_music_upload
allow_intro_video_upload
allow_background_video_upload

READ the actual current schema/UI.

Do NOT create a second Package Manager.

==================================================
8. GLOBAL PACKAGE FEATURES
==================================================

Package Management determines GLOBAL DEFAULT access.

Example:

FREE:
limited features

PROFESSIONAL:
more access

PROFESSIONAL CONCIERGE:
higher Single-owner access

CORPORATE:
global Corporate defaults

Package feature types currently include concepts such as:

BOOLEAN
true / false

NUMERIC
5 / 10 / 30

UNLIMITED
no limit

Preserve/reuse the current flexible package-feature model.

Do NOT represent Unlimited using fake values such as 999999.

==================================================
9. CURRENT PACKAGE MUST CONTROL ACCESS
==================================================

This is critical.

Do NOT permane

--- One-Time Signup Fee @18760 ---
=====================================

When Corporate is selected show:

CORPORATE ACCOUNT SETUP

Company
[________]

Card / Person Limit
[25]

Manage Access
[Configure]

Monthly Subscription
[$200]

One-Time Signup Fee
[$10]

Initial Amount
[$210 automatically calculated]

Future Billing
$200/month

[Create Corporate Account / Generate Payment Link]

Use the existing screen instead of building a separate Corporate creation system.

==================================================
32. CORPORATE STRIPE BILLING
==================================================

The backend should generate the Stripe billing/payment link.

Frontend does NOT create Stripe Products/Prices directly.

Backend uses the Admin-approved Corporate billing terms.

Desired billing:

Initial:
one-time setup/signup fee
+
recurring monthly subscription

Future:
recurring monthly subscription only

Inspect the CURRENT Stripe architecture before implementing.

Reuse:

Stripe customer
Stripe checkout
Stripe payment links
Stripe prices
Stripe subscriptions
Stripe webhooks

where appropriate.

==================================================
33. CORPORATE CUSTOM STRIPE PAYMENT LINK
==================================================

After Admin enters negotiated terms:

[Generate Stripe Payment Link]

Backend creates a customer/agreement-specific Stripe checkout/payment link.

Store/link actual internal metadata such as:

corporateAccountId
packageId
billingAgreementId

Use current naming.

Do NOT trust IDs or prices sent from a customer's browser.

==================================================
34. STRIPE WEBHOOK IS PAYMENT TRUTH
==================================================

Do not mark payment/subscription ACTIVE because the browser reaches a success URL.

Stripe webhook confirmation is the 

--- GLOBAL PAC @5557 ---
e Access
- Agreed Monthly Subscription Amount
- One-Time Signup Fee
- Stripe billing/payment-link area

Corporate enters:

CORPORATE BACK OFFICE

==================================================
7. GLOBAL PACKAGE MANAGEMENT
==================================================

The EXISTING Packages Management area remains the global source of truth.

Current packages include:

Free
Professional
Professional Concierge
Corporate

Current Package Manager already contains things such as:

- Name
- Slug
- Stripe Price ID
- Monthly Price
- Features
- Status
- Order

and current package features such as:

max_social_links
max_extra_fields
allow_video_upload
allow_music_upload
allow_2d_explainer
max_file_size_mb
allow_yt_bg_music_upload
allow_bg_music_upload
allow_intro_video_upload
allow_background_video_upload

READ the actual current schema/UI.

Do NOT create a second Package Manager.

==================================================
8. GLOBAL PACKAGE FEATURES
==================================================

Package Management determines GLOBAL DEFAULT access.

Example:

FREE:
limited features

PROFESSIONAL:
more access

PROFESSIONAL CONCIERGE:
higher Single-owner access

CORPORATE:
global Corporate defaults

Package feature types currently include concepts such as:

BOOLEAN
true / false

NUMERIC
5 / 10 / 30

UNLIMITED
no limit

Preserve/reuse the current flexible package-feature model.

Do NOT represent Unlimited using fake values such as 999999.

==================================================
9. CURRENT PACKAGE MUST CONTROL ACCESS
==================================================

This is critical.

Do NOT permanently copy package permissions into the User when the account is created.

At authentication and protected-action time, the backend must determine the us

--- GLOBAL PAC @6305 ---
music_upload
allow_intro_video_upload
allow_background_video_upload

READ the actual current schema/UI.

Do NOT create a second Package Manager.

==================================================
8. GLOBAL PACKAGE FEATURES
==================================================

Package Management determines GLOBAL DEFAULT access.

Example:

FREE:
limited features

PROFESSIONAL:
more access

PROFESSIONAL CONCIERGE:
higher Single-owner access

CORPORATE:
global Corporate defaults

Package feature types currently include concepts such as:

BOOLEAN
true / false

NUMERIC
5 / 10 / 30

UNLIMITED
no limit

Preserve/reuse the current flexible package-feature model.

Do NOT represent Unlimited using fake values such as 999999.

==================================================
9. CURRENT PACKAGE MUST CONTROL ACCESS
==================================================

This is critical.

Do NOT permanently copy package permissions into the User when the account is created.

At authentication and protected-action time, the backend must determine the user's
CURRENT active/assigned package.

Conceptually:

User
↓
Current Active Subscription / Package Assignment
↓
Package
↓
Package Features
↓
Effective Entitlements
↓
Access

If Admin upgrades:

Professional
→ Professional Concierge

the user should receive Concierge access without manually changing every feature.

If package settings change globally:

users on that package should receive the new effective access.

==================================================
10. CENTRAL ENTITLEMENT SERVICE
==================================================

Create/reuse ONE backend service that is the authority for access.

Conceptually:

getEffectiveEntitlements(userId, profileId?)

SINGLE:

User
→ Current Package
→ Global Package Features

CORPORA

--- $10 @17645 ---
g.

Use the existing Stripe structure.

There is also a globally configured one-time signup/setup fee for applicable paid Single plans.

Current business requirement discussed:

one-time signup fee = $10

Do NOT scatter literal $10 values throughout code.

Store/reuse centralized configuration.

Example:

Professional monthly = package price

FIRST PAYMENT:
one-time signup fee
+
first monthly subscription

FUTURE:
monthly subscription only

Professional Concierge behaves similarly.

Free should not accidentally create paid recurring billing.

==================================================
30. CORPORATE BILLING IS NEGOTIATED
==================================================

Corporate is:

CONTACT FOR SALES

Corporate does not need a fixed public retail price.

Admin negotiates:

- monthly subscription amount
- one-time signup fee
- card/person limit
- feature overrides

Example:

Corporate:
Monthly subscription = $200
Signup Fee = $10
Card Limit = 25

FIRST PAYMENT:
$210

FUTURE MONTHS:
$200/month

==================================================
31. CORPORATE ADMIN CREATION UI
==================================================

When Corporate is selected show:

CORPORATE ACCOUNT SETUP

Company
[________]

Card / Person Limit
[25]

Manage Access
[Configure]

Monthly Subscription
[$200]

One-Time Signup Fee
[$10]

Initial Amount
[$210 automatically calculated]

Future Billing
$200/month

[Create Corporate Account / Generate Payment Link]

Use the existing screen instead of building a separate Corporate creation system.

==================================================
32. CORPORATE STRIPE BILLING
==================================================

The backend should generate the Stripe billing/payment link.

Frontend does NOT create Stripe Products/Prices direct

--- $10 @17673 ---
structure.

There is also a globally configured one-time signup/setup fee for applicable paid Single plans.

Current business requirement discussed:

one-time signup fee = $10

Do NOT scatter literal $10 values throughout code.

Store/reuse centralized configuration.

Example:

Professional monthly = package price

FIRST PAYMENT:
one-time signup fee
+
first monthly subscription

FUTURE:
monthly subscription only

Professional Concierge behaves similarly.

Free should not accidentally create paid recurring billing.

==================================================
30. CORPORATE BILLING IS NEGOTIATED
==================================================

Corporate is:

CONTACT FOR SALES

Corporate does not need a fixed public retail price.

Admin negotiates:

- monthly subscription amount
- one-time signup fee
- card/person limit
- feature overrides

Example:

Corporate:
Monthly subscription = $200
Signup Fee = $10
Card Limit = 25

FIRST PAYMENT:
$210

FUTURE MONTHS:
$200/month

==================================================
31. CORPORATE ADMIN CREATION UI
==================================================

When Corporate is selected show:

CORPORATE ACCOUNT SETUP

Company
[________]

Card / Person Limit
[25]

Manage Access
[Configure]

Monthly Subscription
[$200]

One-Time Signup Fee
[$10]

Initial Amount
[$210 automatically calculated]

Future Billing
$200/month

[Create Corporate Account / Generate Payment Link]

Use the existing screen instead of building a separate Corporate creation system.

==================================================
32. CORPORATE STRIPE BILLING
==================================================

The backend should generate the Stripe billing/payment link.

Frontend does NOT create Stripe Products/Prices directly.

Backend uses the Admin-

--- $10 @18395 ---
xed public retail price.

Admin negotiates:

- monthly subscription amount
- one-time signup fee
- card/person limit
- feature overrides

Example:

Corporate:
Monthly subscription = $200
Signup Fee = $10
Card Limit = 25

FIRST PAYMENT:
$210

FUTURE MONTHS:
$200/month

==================================================
31. CORPORATE ADMIN CREATION UI
==================================================

When Corporate is selected show:

CORPORATE ACCOUNT SETUP

Company
[________]

Card / Person Limit
[25]

Manage Access
[Configure]

Monthly Subscription
[$200]

One-Time Signup Fee
[$10]

Initial Amount
[$210 automatically calculated]

Future Billing
$200/month

[Create Corporate Account / Generate Payment Link]

Use the existing screen instead of building a separate Corporate creation system.

==================================================
32. CORPORATE STRIPE BILLING
==================================================

The backend should generate the Stripe billing/payment link.

Frontend does NOT create Stripe Products/Prices directly.

Backend uses the Admin-approved Corporate billing terms.

Desired billing:

Initial:
one-time setup/signup fee
+
recurring monthly subscription

Future:
recurring monthly subscription only

Inspect the CURRENT Stripe architecture before implementing.

Reuse:

Stripe customer
Stripe checkout
Stripe payment links
Stripe prices
Stripe subscriptions
Stripe webhooks

where appropriate.

==================================================
33. CORPORATE CUSTOM STRIPE PAYMENT LINK
==================================================

After Admin enters negotiated terms:

[Generate Stripe Payment Link]

Backend creates a customer/agreement-specific Stripe checkout/payment link.

Store/link actual internal metadata such as:

corporateAccountId
pack

--- signup fee @17632 ---
 IDs / pricing.

Use the existing Stripe structure.

There is also a globally configured one-time signup/setup fee for applicable paid Single plans.

Current business requirement discussed:

one-time signup fee = $10

Do NOT scatter literal $10 values throughout code.

Store/reuse centralized configuration.

Example:

Professional monthly = package price

FIRST PAYMENT:
one-time signup fee
+
first monthly subscription

FUTURE:
monthly subscription only

Professional Concierge behaves similarly.

Free should not accidentally create paid recurring billing.

==================================================
30. CORPORATE BILLING IS NEGOTIATED
==================================================

Corporate is:

CONTACT FOR SALES

Corporate does not need a fixed public retail price.

Admin negotiates:

- monthly subscription amount
- one-time signup fee
- card/person limit
- feature overrides

Example:

Corporate:
Monthly subscription = $200
Signup Fee = $10
Card Limit = 25

FIRST PAYMENT:
$210

FUTURE MONTHS:
$200/month

==================================================
31. CORPORATE ADMIN CREATION UI
==================================================

When Corporate is selected show:

CORPORATE ACCOUNT SETUP

Company
[________]

Card / Person Limit
[25]

Manage Access
[Configure]

Monthly Subscription
[$200]

One-Time Signup Fee
[$10]

Initial Amount
[$210 automatically calculated]

Future Billing
$200/month

[Create Corporate Account / Generate Payment Link]

Use the existing screen instead of building a separate Corporate creation system.

==================================================
32. CORPORATE STRIPE BILLING
==================================================

The backend should generate the Stripe billing/payment link.

Frontend does NOT create Stripe Products/

--- signup fee @17814 ---
cussed:

one-time signup fee = $10

Do NOT scatter literal $10 values throughout code.

Store/reuse centralized configuration.

Example:

Professional monthly = package price

FIRST PAYMENT:
one-time signup fee
+
first monthly subscription

FUTURE:
monthly subscription only

Professional Concierge behaves similarly.

Free should not accidentally create paid recurring billing.

==================================================
30. CORPORATE BILLING IS NEGOTIATED
==================================================

Corporate is:

CONTACT FOR SALES

Corporate does not need a fixed public retail price.

Admin negotiates:

- monthly subscription amount
- one-time signup fee
- card/person limit
- feature overrides

Example:

Corporate:
Monthly subscription = $200
Signup Fee = $10
Card Limit = 25

FIRST PAYMENT:
$210

FUTURE MONTHS:
$200/month

==================================================
31. CORPORATE ADMIN CREATION UI
==================================================

When Corporate is selected show:

CORPORATE ACCOUNT SETUP

Company
[________]

Card / Person Limit
[25]

Manage Access
[Configure]

Monthly Subscription
[$200]

One-Time Signup Fee
[$10]

Initial Amount
[$210 automatically calculated]

Future Billing
$200/month

[Create Corporate Account / Generate Payment Link]

Use the existing screen instead of building a separate Corporate creation system.

==================================================
32. CORPORATE STRIPE BILLING
==================================================

The backend should generate the Stripe billing/payment link.

Frontend does NOT create Stripe Products/Prices directly.

Backend uses the Admin-approved Corporate billing terms.

Desired billing:

Initial:
one-time setup/signup fee
+
recurring monthly subscription

Future:
recurring m

--- signup fee @18281 ---
==================================================

Corporate is:

CONTACT FOR SALES

Corporate does not need a fixed public retail price.

Admin negotiates:

- monthly subscription amount
- one-time signup fee
- card/person limit
- feature overrides

Example:

Corporate:
Monthly subscription = $200
Signup Fee = $10
Card Limit = 25

FIRST PAYMENT:
$210

FUTURE MONTHS:
$200/month

==================================================
31. CORPORATE ADMIN CREATION UI
==================================================

When Corporate is selected show:

CORPORATE ACCOUNT SETUP

Company
[________]

Card / Person Limit
[25]

Manage Access
[Configure]

Monthly Subscription
[$200]

One-Time Signup Fee
[$10]

Initial Amount
[$210 automatically calculated]

Future Billing
$200/month

[Create Corporate Account / Generate Payment Link]

Use the existing screen instead of building a separate Corporate creation system.

==================================================
32. CORPORATE STRIPE BILLING
==================================================

The backend should generate the Stripe billing/payment link.

Frontend does NOT create Stripe Products/Prices directly.

Backend uses the Admin-approved Corporate billing terms.

Desired billing:

Initial:
one-time setup/signup fee
+
recurring monthly subscription

Future:
recurring monthly subscription only

Inspect the CURRENT Stripe architecture before implementing.

Reuse:

Stripe customer
Stripe checkout
Stripe payment links
Stripe prices
Stripe subscriptions
Stripe webhooks

where appropriate.

==================================================
33. CORPORATE CUSTOM STRIPE PAYMENT LINK
==================================================

After Admin enters negotiated terms:

[Generate Stripe Payment Link]

Backend creates a customer/agree

--- Manage Access @5352 ---
he Admin Create User form expands to show:

CORPORATE ACCOUNT SETUP

Fields should include the current required company/contact fields plus:

- Company / Organization
- Card / Person Creation Limit
- Manage Access
- Agreed Monthly Subscription Amount
- One-Time Signup Fee
- Stripe billing/payment-link area

Corporate enters:

CORPORATE BACK OFFICE

==================================================
7. GLOBAL PACKAGE MANAGEMENT
==================================================

The EXISTING Packages Management area remains the global source of truth.

Current packages include:

Free
Professional
Professional Concierge
Corporate

Current Package Manager already contains things such as:

- Name
- Slug
- Stripe Price ID
- Monthly Price
- Features
- Status
- Order

and current package features such as:

max_social_links
max_extra_fields
allow_video_upload
allow_music_upload
allow_2d_explainer
max_file_size_mb
allow_yt_bg_music_upload
allow_bg_music_upload
allow_intro_video_upload
allow_background_video_upload

READ the actual current schema/UI.

Do NOT create a second Package Manager.

==================================================
8. GLOBAL PACKAGE FEATURES
==================================================

Package Management determines GLOBAL DEFAULT access.

Example:

FREE:
limited features

PROFESSIONAL:
more access

PROFESSIONAL CONCIERGE:
higher Single-owner access

CORPORATE:
global Corporate defaults

Package feature types currently include concepts such as:

BOOLEAN
true / false

NUMERIC
5 / 10 / 30

UNLIMITED
no limit

Preserve/reuse the current flexible package-feature model.

Do NOT represent Unlimited using fake values such as 999999.

==================================================
9. CURRENT PACKAGE MUST CONTROL ACCESS
================================

--- Manage Access @18704 ---
==========
31. CORPORATE ADMIN CREATION UI
==================================================

When Corporate is selected show:

CORPORATE ACCOUNT SETUP

Company
[________]

Card / Person Limit
[25]

Manage Access
[Configure]

Monthly Subscription
[$200]

One-Time Signup Fee
[$10]

Initial Amount
[$210 automatically calculated]

Future Billing
$200/month

[Create Corporate Account / Generate Payment Link]

Use the existing screen instead of building a separate Corporate creation system.

==================================================
32. CORPORATE STRIPE BILLING
==================================================

The backend should generate the Stripe billing/payment link.

Frontend does NOT create Stripe Products/Prices directly.

Backend uses the Admin-approved Corporate billing terms.

Desired billing:

Initial:
one-time setup/signup fee
+
recurring monthly subscription

Future:
recurring monthly subscription only

Inspect the CURRENT Stripe architecture before implementing.

Reuse:

Stripe customer
Stripe checkout
Stripe payment links
Stripe prices
Stripe subscriptions
Stripe webhooks

where appropriate.

==================================================
33. CORPORATE CUSTOM STRIPE PAYMENT LINK
==================================================

After Admin enters negotiated terms:

[Generate Stripe Payment Link]

Backend creates a customer/agreement-specific Stripe checkout/payment link.

Store/link actual internal metadata such as:

corporateAccountId
packageId
billingAgreementId

Use current naming.

Do NOT trust IDs or prices sent from a customer's browser.

==================================================
34. STRIPE WEBHOOK IS PAYMENT TRUTH
==================================================

Do not mark payment/subscription ACTIVE because the browser rea
===== assistant line 3587 =====

--- $10 @2750 ---
). No separate Corporate card table.

## Stripe

Owner Settings checkout: `POST /billing/checkout` (Checkout Session with `price_data`). Signup fee is `Package.signupFeeCents` (cents, not a hardcoded $10). Corporate negotiated monthly is `Subscription.negotiatedMonthlyCents`. Webhook is idempotent (`StripeEvent`). **No Payment Links.** **No `PENDING_PAYMENT` status.** Admin-created paid accounts are activated immediately without Stripe.

## Admin UI

Package Manager is the global catalog (name, slug, prices, signup fee, features, order, status).

Create/edit user: pick package; Corporate extra fields (limit, negotiated monthly, feature override rows) exist on **edit** more than as a dedicated “CORPORATE ACCOUNT SETUP + Generate Payment Link” create flow.

---

# STEP 3 — Gap analysis

**Already matches (do not rebuild):**

- Package selected first; Single vs Corporate is not a separate admin choice.
- One Single back office for Free / Professional / Concierge.
- Central entitlements from current subscription (not copied onto User).
- Corporate overrides stored only as diffs; inherit when no row.
- Per-account Corporate card cap; no delete on reduce.
- Public signup hidden (constant, not a backend flag).
- Owner login OTP; session after OTP; staff password-only.
- Signup fee + monthly quote; webhook idempotency; unpaid Stripe does not unlock paid features.
- Downgrade keeps existing social/extra-field content; new writes over the cap are rejected.

**Missing or only partial vs this prompt:**

| Target | Gap |
|---|---|
| Persist `ownerMode` on Package | Still slug/name heuristics (`if corporate…`) |
| Role = ADMIN vs USER | Roles remain `vcard-owner` / `corporate-owner` (package is mixed into role) |
| `publicSignupEnabled` reversible flag | Frontend constant only; regis

--- $10 @5597 ---
r self-serve only |
| Don’t leak “email exists” on public forgot/login | Forgot password currently 404s if user missing |
| OTP ~10 minutes | Driven by `OTP_EXPIRE_MINUTES` (example is 2) |
| Central $10 signup fee | Per-package `signupFeeCents` (correct pattern; catalog values must be set, not literals) |

**Do not do:** drop tables, duplicate Package Manager, duplicate Profile, wipe Stripe/history, auto-delete cards/links.

---

# STEP 4 — Numbered work plan

Approve or mark steps to skip/change. After approval I will implement **step 1 only**, then stop.

### 1. Auth changes

- **Objective:** Reversible public-signup flag; keep register API but gated; admin-created users use setup email + OTP, not a lasting admin password; tighten OTP timing/enumeration where it is cheap and safe.
- **Files:** `publicSignup.ts`, register page/layout, `auth.route.ts`, `auth.service.ts`, `authChallenge.*`, config/`OTP_EXPIRE_MINUTES`, forgot-password errors.
- **DB:** none required (`AuthChallenge` / `PasswordSetupToken` already exist).
- **Risks:** OTP mail failure locks owners (rollback: `LOGIN_OTP_REQUIRED=false`). Enumeration change may surprise ops who rely on “user not found”.
- **Depends on:** none.

### 2. Package / account-type changes

- **Objective:** Store `Package.ownerMode` (`single` | `corporate`), backfill from current slug rules, resolve mode from column with slug fallback.
- **Files:** `prisma/schema.prisma`, migration, `packageOwnerMode.ts` (both repos), `adminPackage.service.ts` / Admin Packages UI (read-only badge or editable with guardrails).
- **DB:** yes — nullable or defaulted `Package.ownerMode`.
- **Risks:** Wrong backfill would send Corporate users to Single (or the reverse). Dry-run report first.
- **Depends on:** none (can run parallel to 1).

### 3. Admin

--- signup fee @3064 ---
. **No Payment Links.** **No `PENDING_PAYMENT` status.** Admin-created paid accounts are activated immediately without Stripe.

## Admin UI

Package Manager is the global catalog (name, slug, prices, signup fee, features, order, status).

Create/edit user: pick package; Corporate extra fields (limit, negotiated monthly, feature override rows) exist on **edit** more than as a dedicated “CORPORATE ACCOUNT SETUP + Generate Payment Link” create flow.

---

# STEP 3 — Gap analysis

**Already matches (do not rebuild):**

- Package selected first; Single vs Corporate is not a separate admin choice.
- One Single back office for Free / Professional / Concierge.
- Central entitlements from current subscription (not copied onto User).
- Corporate overrides stored only as diffs; inherit when no row.
- Per-account Corporate card cap; no delete on reduce.
- Public signup hidden (constant, not a backend flag).
- Owner login OTP; session after OTP; staff password-only.
- Signup fee + monthly quote; webhook idempotency; unpaid Stripe does not unlock paid features.
- Downgrade keeps existing social/extra-field content; new writes over the cap are rejected.

**Missing or only partial vs this prompt:**

| Target | Gap |
|---|---|
| Persist `ownerMode` on Package | Still slug/name heuristics (`if corporate…`) |
| Role = ADMIN vs USER | Roles remain `vcard-owner` / `corporate-owner` (package is mixed into role) |
| `publicSignupEnabled` reversible flag | Frontend constant only; register API still open |
| Admin create: no permanent admin password; email verify + set password + OTP | Create still hashes a password and sets `isVerified: true` |
| Corporate create: company, limit, Manage Access popup, negotiated $ , signup fee, **Generate Payment Link**, auto $ first invoice | Partial on edit; 

--- signup fee @4585 ---
: no permanent admin password; email verify + set password + OTP | Create still hashes a password and sets `isVerified: true` |
| Corporate create: company, limit, Manage Access popup, negotiated $ , signup fee, **Generate Payment Link**, auto $ first invoice | Partial on edit; **no Payment Link**; create often activates paid access immediately |
| `PENDING_PAYMENT` then webhook activates same user | Admin/Stripe path activates or uses `incomplete`; no first-class pending |
| Entitlements include **all** catalog features (`allow_video_upload`, `allow_2d_explainer`, …) | Only seven `allow_*` keys are enforced/exposed |
| `FEATURE_NOT_INCLUDED` / `FEATURE_LIMIT_REACHED` / `CORPORATE_CARD_LIMIT_REACHED` | Uses `PACKAGE_FEATURE_LOCKED` / `PACKAGE_LIMIT_REACHED` |
| `GET /me` or `/me/entitlements` | `GET /profiles/entitlements` |
| Backend enforce media flags (2D explainer, background video, etc.) | Not gated by those keys |
| Stripe Payment Links from admin, metadata on agreement | Checkout Session for self-serve only |
| Don’t leak “email exists” on public forgot/login | Forgot password currently 404s if user missing |
| OTP ~10 minutes | Driven by `OTP_EXPIRE_MINUTES` (example is 2) |
| Central $10 signup fee | Per-package `signupFeeCents` (correct pattern; catalog values must be set, not literals) |

**Do not do:** drop tables, duplicate Package Manager, duplicate Profile, wipe Stripe/history, auto-delete cards/links.

---

# STEP 4 — Numbered work plan

Approve or mark steps to skip/change. After approval I will implement **step 1 only**, then stop.

### 1. Auth changes

- **Objective:** Reversible public-signup flag; keep register API but gated; admin-created users use setup email + OTP, not a lasting admin password; tighten OTP timing/enumeration where it is cheap and

--- signup fee @5601 ---
lf-serve only |
| Don’t leak “email exists” on public forgot/login | Forgot password currently 404s if user missing |
| OTP ~10 minutes | Driven by `OTP_EXPIRE_MINUTES` (example is 2) |
| Central $10 signup fee | Per-package `signupFeeCents` (correct pattern; catalog values must be set, not literals) |

**Do not do:** drop tables, duplicate Package Manager, duplicate Profile, wipe Stripe/history, auto-delete cards/links.

---

# STEP 4 — Numbered work plan

Approve or mark steps to skip/change. After approval I will implement **step 1 only**, then stop.

### 1. Auth changes

- **Objective:** Reversible public-signup flag; keep register API but gated; admin-created users use setup email + OTP, not a lasting admin password; tighten OTP timing/enumeration where it is cheap and safe.
- **Files:** `publicSignup.ts`, register page/layout, `auth.route.ts`, `auth.service.ts`, `authChallenge.*`, config/`OTP_EXPIRE_MINUTES`, forgot-password errors.
- **DB:** none required (`AuthChallenge` / `PasswordSetupToken` already exist).
- **Risks:** OTP mail failure locks owners (rollback: `LOGIN_OTP_REQUIRED=false`). Enumeration change may surprise ops who rely on “user not found”.
- **Depends on:** none.

### 2. Package / account-type changes

- **Objective:** Store `Package.ownerMode` (`single` | `corporate`), backfill from current slug rules, resolve mode from column with slug fallback.
- **Files:** `prisma/schema.prisma`, migration, `packageOwnerMode.ts` (both repos), `adminPackage.service.ts` / Admin Packages UI (read-only badge or editable with guardrails).
- **DB:** yes — nullable or defaulted `Package.ownerMode`.
- **Risks:** Wrong backfill would send Corporate users to Single (or the reverse). Dry-run report first.
- **Depends on:** none (can run parallel to 1).

### 3. Admin Cre

--- Manage Access @4549 ---
ster API still open |
| Admin create: no permanent admin password; email verify + set password + OTP | Create still hashes a password and sets `isVerified: true` |
| Corporate create: company, limit, Manage Access popup, negotiated $ , signup fee, **Generate Payment Link**, auto $ first invoice | Partial on edit; **no Payment Link**; create often activates paid access immediately |
| `PENDING_PAYMENT` then webhook activates same user | Admin/Stripe path activates or uses `incomplete`; no first-class pending |
| Entitlements include **all** catalog features (`allow_video_upload`, `allow_2d_explainer`, …) | Only seven `allow_*` keys are enforced/exposed |
| `FEATURE_NOT_INCLUDED` / `FEATURE_LIMIT_REACHED` / `CORPORATE_CARD_LIMIT_REACHED` | Uses `PACKAGE_FEATURE_LOCKED` / `PACKAGE_LIMIT_REACHED` |
| `GET /me` or `/me/entitlements` | `GET /profiles/entitlements` |
| Backend enforce media flags (2D explainer, background video, etc.) | Not gated by those keys |
| Stripe Payment Links from admin, metadata on agreement | Checkout Session for self-serve only |
| Don’t leak “email exists” on public forgot/login | Forgot password currently 404s if user missing |
| OTP ~10 minutes | Driven by `OTP_EXPIRE_MINUTES` (example is 2) |
| Central $10 signup fee | Per-package `signupFeeCents` (correct pattern; catalog values must be set, not literals) |

**Do not do:** drop tables, duplicate Package Manager, duplicate Profile, wipe Stripe/history, auto-delete cards/links.

---

# STEP 4 — Numbered work plan

Approve or mark steps to skip/change. After approval I will implement **step 1 only**, then stop.

### 1. Auth changes

- **Objective:** Reversible public-signup flag; keep register API but gated; admin-created users use setup email + OTP, not a lasting admin password; tighten OTP timi

--- Manage Access @7340 ---
Depends on:** none (can run parallel to 1).

### 3. Admin Create User changes

- **Objective:** Create form = select package only. Single packages: identity + package. Corporate: company, card limit, Manage Access (inherit vs override for **all** Corporate features), negotiated monthly, signup fee display, first-invoice total, **Generate payment link** (after 7). No admin-chosen Single/Corporate toggle. Stop requiring a permanent password; send setup/activate mail.
- **Files:** `AdminUsers.tsx`, `adminUser.service.ts` / zod, `subscription.service.ts`.
- **DB:** none if using existing User + Subscription + `CorporateFeatureOverride`.
- **Risks:** Creating Corporate as `ACTIVE` before pay contradicts §35. Prefer pending until 7.
- **Depends on:** 1 (activation), 2 (ownerMode), 5 (override UI), 7 (payment link).

### 4. Entitlement service

- **Objective:** One resolver remains `getEffectiveEntitlements`. Expand **effective features** to the full `PackageFeature` set (boolean / numeric / unlimited), not only the seven keys. Add `backOffice`, `subscriptionStatus` (`active` | `pending_payment` | `inactive`), Corporate `limit/used/remaining`. Keep resolving live from subscription (no copy onto User). Optional alias `GET /profiles/me/entitlements` or document `/profiles/entitlements` as `/me`.
- **Files:** `effectiveEntitlements.ts`, `entitlement.service.ts`, `profiles.api.ts`, `usePackageAccess.ts`.
- **DB:** none.
- **Risks:** Missing flags today mean “allowed”; exposing all keys may lock media features that were never flagged in catalog. Default missing `allow_*` stays allowed unless catalog has `0`.
- **Depends on:** 2 (ownerMode), 7 (pending/paid status).

### 5. Corporate access overrides

- **Objective:** Manage Access popup: every Corporate package feature, Global vs T

--- Manage Access @8875 ---
ever flagged in catalog. Default missing `allow_*` stays allowed unless catalog has `0`.
- **Depends on:** 2 (ownerMode), 7 (pending/paid status).

### 5. Corporate access overrides

- **Objective:** Manage Access popup: every Corporate package feature, Global vs This account (`Use global` / Yes / No / custom / unlimited). Persist **only** override rows. `max_cards` stays on `Subscription.quantity`, not overrides.
- **Files:** `AdminUsers.tsx` (create + edit), `corporateFeatureOverrides.ts`, `adminUser.service.ts`.
- **DB:** none (`CorporateFeatureOverride` exists).
- **Risks:** Saving a full copy of globals would break inheritance — UI must omit “use global” keys.
- **Depends on:** 4 (feature list from Corporate package).

### 6. Corporate card limit

- **Objective:** Keep `Subscription.quantity`. Structured error `CORPORATE_CARD_LIMIT_REACHED` (keep or alias current capacity error). Directory already uses `companyUserId`. Show limit / used / remaining in Corporate back office.
- **Files:** `profile.service.ts` (`assertCanCreateCard`, `getCardCapacity`), Corporate dashboard widgets, entitlements payload.
- **DB:** none.
- **Risks:** Counting the wrong profiles (`userId` vs `companyUserId`) under- or over-enforces. Do not delete cards when lowering quantity.
- **Depends on:** 4.

### 7. Stripe billing

- **Objective:** Single paid: global `monthlyPrice` + `signupFeeCents` on first invoice only (no per-user negotiated monthly). Corporate: admin terms → backend **Payment Link or Checkout URL** (never prices from the browser). Create/configure account as `pending_payment` (`provider: stripe`, `stripeStatus: incomplete` or explicit status). Webhook activates **the same** user/subscription. Idempotent. Failed pay does not activate. No duplicate accounts.
- **Files:** `stripe
