=== USER LINE 3583 len 25814 ===
--- user SIGNUP at 3549 ---
SIGNUP FOR NOW
==================================================

For now the public Auth UI should expose only:

- Login
- Forgot Password
- Reset Password

Hide:

- Signup
- Register
- Create Account

Do NOT delete signup/registration backend capability permanently.

We may expose public signup later.

Prefer a reversible configuration/feature flag if the architecture supports it.

Example concept:

publicSignupEnabled = false

Direct navigation to public Signup while disabled should safely redirect to Login
or follow the current secure routing convention.

==================================================
4. ADMIN CREATES USERS
==================================================

For now users are created by Admin.

Admin Create User should directly show:

SELECT PACKAGE

○ Free
○ Professional
○ Professional Concierge
○ Corporate

No separate Single/Corporate selection.

Package choice changes the creation form dynamically.

==================================================
5. SINGLE USER CREATION
==================================================

If Admin selects:

Free
Professional
Professional Concierge

create/reuse:

User
+
Single Card Owner relationship if needed
+
selected Package / Subscription relationship
+
current Profile/card structure

All three users enter:

SINGLE BACK OFFICE

The difference between them is PACKAGE ACCESS.

Do not build different Single dashboards for Free/Professional/Concierge.

==================================================
6. CORPORATE USER CREATION
==================================================

If Admin selects:

Corporate

the Admin Create User form expands to show:

CORPORATE ACCOUNT SETUP

Fields should include the current required company/contact fields plus:

- Company / Organization
- Card / Person Creation Limit

--- user signup at 3767 ---
signup/registration backend capability permanently.

We may expose public signup later.

Prefer a reversible configuration/feature flag if the architecture supports it.

Example concept:

publicSignupEnabled = false

Direct navigation to public Signup while disabled should safely redirect to Login
or follow the current secure routing convention.

==================================================
4. ADMIN CREATES USERS
==================================================

For now users are created by Admin.

Admin Create User should directly show:

SELECT PACKAGE

○ Free
○ Professional
○ Professional Concierge
○ Corporate

No separate Single/Corporate selection.

Package choice changes the creation form dynamically.

==================================================
5. SINGLE USER CREATION
==================================================

If Admin selects:

Free
Professional
Professional Concierge

create/reuse:

User
+
Single Card Owner relationship if needed
+
selected Package / Subscription relationship
+
current Profile/card structure

All three users enter:

SINGLE BACK OFFICE

The difference between them is PACKAGE ACCESS.

Do not build different Single dashboards for Free/Professional/Concierge.

==================================================
6. CORPORATE USER CREATION
==================================================

If Admin selects:

Corporate

the Admin Create User form expands to show:

CORPORATE ACCOUNT SETUP

Fields should include the current required company/contact fields plus:

- Company / Organization
- Card / Person Creation Limit
- Manage Access
- Agreed Monthly Subscription Amount
- One-Time Signup Fee
- Stripe billing/payment-link area

Corporate enters:

CORPORATE BACK OFFICE

==================================================
7. GLOBAL PAC

--- user Payment Link at 18899 ---
Payment Link]

Use the existing screen instead of building a separate Corporate creation system.

==================================================
32. CORPORATE STRIPE BILLING
==================================================

The backend should generate the Stripe billing/payment link.

Frontend does NOT create Stripe Products/Prices directly.

Backend uses the Admin-approved Corporate billing terms.

Desired billing:

Initial:
one-time setup/signup fee
+
recurring monthly subscription

Future:
recurring monthly subscription only

Inspect the CURRENT Stripe architecture before implementing.

Reuse:

Stripe customer
Stripe checkout
Stripe payment links
Stripe prices
Stripe subscriptions
Stripe webhooks

where appropriate.

==================================================
33. CORPORATE CUSTOM STRIPE PAYMENT LINK
==================================================

After Admin enters negotiated terms:

[Generate Stripe Payment Link]

Backend creates a customer/agreement-specific Stripe checkout/payment link.

Store/link actual internal metadata such as:

corporateAccountId
packageId
billingAgreementId

Use current naming.

Do NOT trust IDs or prices sent from a customer's browser.

==================================================
34. STRIPE WEBHOOK IS PAYMENT TRUTH
==================================================

Do not mark payment/subscription ACTIVE because the browser reaches a success URL.

Stripe webhook confirmation is the source of truth.

Webhook handling must be idempotent.

Repeated webhook delivery must NOT create:

- duplicate Corporate account
- duplicate subscription
- duplicate payment
- duplicate package assignment

==================================================
35. CORPORATE BEFORE PAYMENT
==================================================



--- user Corporate at 162 ---
Corporate access-control, Corporate card-limit, Admin user-creation, and Stripe billing architecture.

Repositories:
- Frontend/Admin: vbiz-me-administration
- Backend/API: vbiz-me-backend

THIS IS AN EXISTING PRODUCTION SYSTEM.

DO NOT immediately start coding.

==================================================
MANDATORY WORKING METHOD
==================================================

FIRST:

STEP 1 — READ
Inspect the existing frontend, backend, Prisma schema, migrations, authentication,
packages, subscriptions, Corporate structures, Stripe integration, feature access,
Admin user creation, and routing.

STEP 2 — MAP
Explain how the CURRENT system works.

STEP 3 — GAP ANALYSIS
Compare the current implementation with the target architecture in this prompt.

STEP 4 — CREATE A NUMBERED WORK PLAN
Before modifying code, give me a numbered implementation plan such as:

1. Auth changes
2. Package/account-type changes
3. Admin Create User changes
4. Entitlement service
5. Corporate access overrides
6. Corporate card limit
7. Stripe billing
8. Routing
9. Frontend enforcement
10. Backend enforcement
11. Migration/backfill
12. Tests
13. Build/deployment

For every step list:
- files/components/services likely involved
- database changes if any
- risks
- dependencies on earlier steps

THEN STOP AND SHOW ME THE PLAN.

After the plan is approved, work STEP BY STEP IN ORDER.

When executing:
- finish one logical step
- report what changed
- run relevant tests/typecheck
- then continue to the next step

Do NOT make uncontrolled changes across the whole repository at once.

==================================================
CORE BUSINESS MODEL
==================================================

vBiz Me has FOUR packages:

1. Free
2. Professional
3. Professional Concierge
4. Corporate

=== USER LINE 3584 len 25814 ===
--- user SIGNUP at 3549 ---
SIGNUP FOR NOW
==================================================

For now the public Auth UI should expose only:

- Login
- Forgot Password
- Reset Password

Hide:

- Signup
- Register
- Create Account

Do NOT delete signup/registration backend capability permanently.

We may expose public signup later.

Prefer a reversible configuration/feature flag if the architecture supports it.

Example concept:

publicSignupEnabled = false

Direct navigation to public Signup while disabled should safely redirect to Login
or follow the current secure routing convention.

==================================================
4. ADMIN CREATES USERS
==================================================

For now users are created by Admin.

Admin Create User should directly show:

SELECT PACKAGE

○ Free
○ Professional
○ Professional Concierge
○ Corporate

No separate Single/Corporate selection.

Package choice changes the creation form dynamically.

==================================================
5. SINGLE USER CREATION
==================================================

If Admin selects:

Free
Professional
Professional Concierge

create/reuse:

User
+
Single Card Owner relationship if needed
+
selected Package / Subscription relationship
+
current Profile/card structure

All three users enter:

SINGLE BACK OFFICE

The difference between them is PACKAGE ACCESS.

Do not build different Single dashboards for Free/Professional/Concierge.

==================================================
6. CORPORATE USER CREATION
==================================================

If Admin selects:

Corporate

the Admin Create User form expands to show:

CORPORATE ACCOUNT SETUP

Fields should include the current required company/contact fields plus:

- Company / Organization
- Card / Person Creation Limit

--- user signup at 3767 ---
signup/registration backend capability permanently.

We may expose public signup later.

Prefer a reversible configuration/feature flag if the architecture supports it.

Example concept:

publicSignupEnabled = false

Direct navigation to public Signup while disabled should safely redirect to Login
or follow the current secure routing convention.

==================================================
4. ADMIN CREATES USERS
==================================================

For now users are created by Admin.

Admin Create User should directly show:

SELECT PACKAGE

○ Free
○ Professional
○ Professional Concierge
○ Corporate

No separate Single/Corporate selection.

Package choice changes the creation form dynamically.

==================================================
5. SINGLE USER CREATION
==================================================

If Admin selects:

Free
Professional
Professional Concierge

create/reuse:

User
+
Single Card Owner relationship if needed
+
selected Package / Subscription relationship
+
current Profile/card structure

All three users enter:

SINGLE BACK OFFICE

The difference between them is PACKAGE ACCESS.

Do not build different Single dashboards for Free/Professional/Concierge.

==================================================
6. CORPORATE USER CREATION
==================================================

If Admin selects:

Corporate

the Admin Create User form expands to show:

CORPORATE ACCOUNT SETUP

Fields should include the current required company/contact fields plus:

- Company / Organization
- Card / Person Creation Limit
- Manage Access
- Agreed Monthly Subscription Amount
- One-Time Signup Fee
- Stripe billing/payment-link area

Corporate enters:

CORPORATE BACK OFFICE

==================================================
7. GLOBAL PAC

--- user Payment Link at 18899 ---
Payment Link]

Use the existing screen instead of building a separate Corporate creation system.

==================================================
32. CORPORATE STRIPE BILLING
==================================================

The backend should generate the Stripe billing/payment link.

Frontend does NOT create Stripe Products/Prices directly.

Backend uses the Admin-approved Corporate billing terms.

Desired billing:

Initial:
one-time setup/signup fee
+
recurring monthly subscription

Future:
recurring monthly subscription only

Inspect the CURRENT Stripe architecture before implementing.

Reuse:

Stripe customer
Stripe checkout
Stripe payment links
Stripe prices
Stripe subscriptions
Stripe webhooks

where appropriate.

==================================================
33. CORPORATE CUSTOM STRIPE PAYMENT LINK
==================================================

After Admin enters negotiated terms:

[Generate Stripe Payment Link]

Backend creates a customer/agreement-specific Stripe checkout/payment link.

Store/link actual internal metadata such as:

corporateAccountId
packageId
billingAgreementId

Use current naming.

Do NOT trust IDs or prices sent from a customer's browser.

==================================================
34. STRIPE WEBHOOK IS PAYMENT TRUTH
==================================================

Do not mark payment/subscription ACTIVE because the browser reaches a success URL.

Stripe webhook confirmation is the source of truth.

Webhook handling must be idempotent.

Repeated webhook delivery must NOT create:

- duplicate Corporate account
- duplicate subscription
- duplicate payment
- duplicate package assignment

==================================================
35. CORPORATE BEFORE PAYMENT
==================================================



--- user Corporate at 162 ---
Corporate access-control, Corporate card-limit, Admin user-creation, and Stripe billing architecture.

Repositories:
- Frontend/Admin: vbiz-me-administration
- Backend/API: vbiz-me-backend

THIS IS AN EXISTING PRODUCTION SYSTEM.

DO NOT immediately start coding.

==================================================
MANDATORY WORKING METHOD
==================================================

FIRST:

STEP 1 — READ
Inspect the existing frontend, backend, Prisma schema, migrations, authentication,
packages, subscriptions, Corporate structures, Stripe integration, feature access,
Admin user creation, and routing.

STEP 2 — MAP
Explain how the CURRENT system works.

STEP 3 — GAP ANALYSIS
Compare the current implementation with the target architecture in this prompt.

STEP 4 — CREATE A NUMBERED WORK PLAN
Before modifying code, give me a numbered implementation plan such as:

1. Auth changes
2. Package/account-type changes
3. Admin Create User changes
4. Entitlement service
5. Corporate access overrides
6. Corporate card limit
7. Stripe billing
8. Routing
9. Frontend enforcement
10. Backend enforcement
11. Migration/backfill
12. Tests
13. Build/deployment

For every step list:
- files/components/services likely involved
- database changes if any
- risks
- dependencies on earlier steps

THEN STOP AND SHOW ME THE PLAN.

After the plan is approved, work STEP BY STEP IN ORDER.

When executing:
- finish one logical step
- report what changed
- run relevant tests/typecheck
- then continue to the next step

Do NOT make uncontrolled changes across the whole repository at once.

==================================================
CORE BUSINESS MODEL
==================================================

vBiz Me has FOUR packages:

1. Free
2. Professional
3. Professional Concierge
4. Corporate

=== LINE 3587 len 14244 ===
--- 13. Build at 13373 ---
13. Build / deployment

- **Objective:** `CUTOVER.md` order: migrate → backfill dry-run → apply → backend → admin → Stripe webhook URL → smoke (staff login, owner OTP, Single `/`, Corporate `/teamvcard`, package edit, card cap). Rollback OTP: `LOGIN_OTP_REQUIRED=false`.
- **Files:** `CUTOVER.md`, `cutover:check`, env examples.
- **DB:** `migrate deploy` only.
- **Depends on:** all previous.

---

## Recommended constraint

Do **not** collapse `vcard-owner` / `corporate-owner` into a single `USER` role in this program unless you explicitly approve that as its own step — it is authorization, not package, and it touches almost every guard.

Do **not** ship Payment Links (7) before pending Corporate create (3) and entitlements status (4).

---

Approve this plan (or mark steps to skip/change). After approval I will implement **step 1 only**, then report and wait.

--- Payment Link at 2871 ---
Payment Links.** **No `PENDING_PAYMENT` status.** Admin-created paid accounts are activated immediately without Stripe.

## Admin UI

Package Manager is the global catalog (name, slug, prices, signup fee, features, order, status).

Create/edit user: pick package; Corporate extra fields (limit, negotiated monthly, feature override rows) exist on **edit** more than as a dedicated “CORPORATE ACCOUNT SETUP + Generate Payment Link” create flow.

---

# STEP 3 — Gap analysis

**Already matches (do not rebuild):**

- Package selected first; Single vs Corporate is not a separate admin choice.
- One Single back office for Free / Professional / Concierge.
- Central entitlements from current subscription (not copied onto User).
- Corporate overrides stored only as diffs; inherit when no row.
- Per-account Corporate card cap; no delete on reduce.
- Public signup hidden (constant, not a backend flag).
- Owner login OTP; session after OTP; staff password-only.
- Signup fee + monthly quote; webhook idempotency; unpaid Stripe does not unlock paid features.
- Downgrade keeps existing social/extra-field content; new writes over the cap are rejected.

**Missing or only partial vs this prompt:**

| Target | Gap |
|---|---|
| Persist `ownerMode` on Package | Still slug/name heuristics (`if corporate…`) |
| Role = ADMIN vs USER | Roles remain `vcard-owner` / `corporate-owner` (package is mixed into role) |
| `publicSignupEnabled` reversible flag | Frontend constant only; register API still open |
| Admin create: no permanent admin password; email verify + set password + OTP | Create still hashes a password and sets `isVerified: true` |
| Corporate create: company, limit, Manage Access popup, negotiated $ , signup fee, **Generate Payment Link**, auto $ first invoice | Partial on edit; **no Payment Link**; create often activates paid access immediately |
| `PENDING_PAYMENT` then webhook activates same user | Admin/Stripe path activates or uses `incomplete`; no first-class pending |
| Entitlements include **all** catalog features (`allow_video_upload`, `allow_2d_explainer`, …) | Only seven `allow_*` keys are enforced/exposed |
| `FEATURE_NOT_INCLUDED` / `FEATURE_LIMIT_REACHED` / `CORPORATE_CARD_LIMIT_REACHED` | Uses `PACKAGE_FEATURE_LOCKED` / `PACKAGE_LIMIT_REACHED` |
| `GET /me` or `/me/entitlements` | `GET /profiles/entitlements` |
| Backend enforce media flags (2D explainer, background video, etc.) | Not gated by those keys |
| Stripe Payment Links from admin, metadata on agreement | Checkout Session for self-serve only |
| Don’t leak “email exists” on public forgot/login | Forgot password currently 404s if user missing |
| OTP ~10 minutes | Driven by `OTP_EXPIRE_MINUTES` (example is 2) |
| Central $10 signup fee | Per-package `signupFeeCents` (correct pattern; catalog va

--- signup fee at 3064 ---
signup fee, features, order, status).

Create/edit user: pick package; Corporate extra fields (limit, negotiated monthly, feature override rows) exist on **edit** more than as a dedicated “CORPORATE ACCOUNT SETUP + Generate Payment Link” create flow.

---

# STEP 3 — Gap analysis

**Already matches (do not rebuild):**

- Package selected first; Single vs Corporate is not a separate admin choice.
- One Single back office for Free / Professional / Concierge.
- Central entitlements from current subscription (not copied onto User).
- Corporate overrides stored only as diffs; inherit when no row.
- Per-account Corporate card cap; no delete on reduce.
- Public signup hidden (constant, not a backend flag).
- Owner login OTP; session after OTP; staff password-only.
- Signup fee + monthly quote; webhook idempotency; unpaid Stripe does not unlock paid features.
- Downgrade keeps existing social/extra-field content; new writes over the cap are rejected.

**Missing or only partial vs this prompt:**

| Target | Gap |
|---|---|
| Persist `ownerMode` on Package | Still slug/name heuristics (`if corporate…`) |
| Role = ADMIN vs USER | Roles remain `vcard-owner` / `corporate-owner` (package is mixed into role) |
| `publicSignupEnabled` reversible flag | Frontend constant only; register API still open |
| Admin create: no permanent admin password; email verify + set password + OTP | Create still hashes a password and sets `isVerified: true` |
| Corporate create: company, limit, Manage Access popup, negotiated $ , signup fee, **Generate Payment Link**, auto $ first invoice | Partial on edit; **no Payment Link**; create often activates paid access immediately |
| `PENDING_PAYMENT` then webhook activates same user | Admin/Stripe path activates or uses `incomplete`; no first-class pending |
| Entitlements include **all** catalog features (`allow_video_upload`, `allow_2d_explainer`, …) | Only seven `allow_*` keys are enforced/exposed |
| `FEATURE_NOT_INCLUDED` / `FEATURE_LIMIT_REACHED` / `CORPORATE_CARD_LIMIT_REACHED` | Uses `PACKAGE_FEATURE_LOCKED` / `PACKAGE_LIMIT_REACHED` |
| `GET /me` or `/me/entitlements` | `GET /profiles/entitlements` |
| Backend enforce media flags (2D explainer, background video, etc.) | Not gated by those keys |
| Stripe Payment Links from admin, metadata on agreement | Checkout Session for self-serve only |
| Don’t leak “email exists” on public forgot/login | Forgot password currently 404s if user missing |
| OTP ~10 minutes | Driven by `OTP_EXPIRE_MINUTES` (example is 2) |
| Central $10 signup fee | Per-package `signupFeeCents` (correct pattern; catalog values must be set, not literals) |

**Do not do:** drop tables, duplicate Package Manager, duplicate Profile, wipe Stripe/history, auto-delete cards/links.

---

# STEP 4 — Numbered work plan

A

FOUND 1
